<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="survey_question_form" model="ir.ui.view">
        <field name="name">survey.question.form.inherit</field>
        <field name="model">survey.question</field>
        <field name="inherit_id" ref="survey.survey_question_form" />
        <field name="arch" type="xml">

            <xpath expr="//div[@class='oe_title']" position="after">
                <group
                    name="scoring_based_on_pages_group"
                    string="Section-specific Score Limit"
                    attrs="{'invisible': [('is_page', '=', False)] }"
                >
                    <field name="scoring_based_on_pages" invisible="1" />
                    <field name="page_minimum_required_score" />
                </group>
            </xpath>

        </field>
    </record>
</odoo>
