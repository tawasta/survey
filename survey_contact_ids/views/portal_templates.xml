<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template
        id="portal_my_survey_answers_contacts"
        inherit_id="survey_portal.portal_my_survey_answers"
        name="My Survey Answers"
    >
        <xpath expr="//th[@id='th-create-date']" position="after">
            <th id="th-contacts">Contacts</th>
        </xpath>
        <xpath expr="//td[@id='td-create-date']" position="after">
            <td id="td-contacts">
                <t t-set="contact_count" t-value="len(answer.contact_ids)" />
                <t t-set="current_count" t-value="0" />
                <t
                    t-foreach="answer.contact_ids.sorted(key=lambda x: x.name)"
                    t-as="contact"
                >
                    <t t-set="current_count" t-value="current_count +1" />
                    <span
                        t-field="contact.name"
                        data-toggle="tooltip"
                        t-att-title="contact.name"
                    /><t t-if="contact_count - current_count != 0">, <br /></t>
                </t>
            </td>
        </xpath>
        <xpath expr="//a[@id='button-review-answer']" position="before">
            <button
                type="button"
                id="modal_invite_contacts"
                class="btn btn-primary btn-sm"
                t-att-answer-id="answer.id"
                t-att-data-token="answer.access_token"
                t-att-data-survey="answer.survey_id.access_token"
            >
                    <i class="fa fa-address-book" /> Add Contacts
                </button>
        </xpath>
    </template>
    <template id="invite_contacts_modal" name="Invite Contacts Modal">
        <div id="modal_invite_contacts" class="modal fade" tabindex="-1" role="dialog">
            <div class="modal-dialog modal-lg" role="document">
                <form
                    id="survey_invite_form"
                    class="mt32"
                    t-att-action="'/survey/contacts/%s/%s/post' % (survey_token, answer_token)"
                    method="post"
                    enctype="multipart/form-data"
                >
                  <input
                        type="hidden"
                        name="csrf_token"
                        t-att-value="request.csrf_token(time_limit=False)"
                    />
                  <div class="modal-content">
                    <div class="modal-header align-items-center">
                      <h4 class="modal-title">Add Contacts</h4>
                      <button
                                type="button"
                                class="btn-close btn-close-white"
                                data-bs-dismiss="modal"
                                aria-label="Close"
                            />
                    </div>
                    <div class="modal-body">
                      <div class="form-group">
                        <label for="email" class="form-label">Email</label>
                        <input
                                    name="email"
                                    class="form-control"
                                    type="email"
                                    required="required"
                                />
                      </div>
                      <div class="alert alert-info mt-3">
                        <i class="fa fa-info-circle" />
                        The invited user will gain access to this survey answer.
                      </div>
                    </div>
                    <div class="modal-footer">
                      <button
                                type="button"
                                class="btn btn-secondary"
                                data-bs-dismiss="modal"
                            >Cancel</button>
                      <button type="submit" class="btn btn-primary">Add Contact</button>
                    </div>
                  </div>
                </form>
            </div>
        </div>
    </template>
    <template
        id="portal_answer_page_contact_ids"
        name="Survey Answer Portal Template"
        inherit_id="survey_portal.portal_answer_page"
    >
        <xpath expr="//li[@id='sidebar_partner']" position="after">
            <li
                id="sidebar_contact"
                t-if="answer.contact_ids"
                class="list-group-item flex-grow-1"
            >
                <div class="small mb-1"><strong
                        class="text-muted"
                    >Contacts</strong></div>
                <t t-foreach="answer.contact_ids" t-as="contact">
                    <div class="row">
                        <div class="col flex-grow-0 pr-2">
                            <img
                                t-if="contact.image_1024"
                                class="rounded-circle mt-1 o_portal_contact_img"
                                t-att-src="image_data_uri(contact.image_1024)"
                                alt="Contact"
                            />
                            <img
                                t-else=""
                                class="rounded-circle mt-1 o_portal_contact_img"
                                src="/web/static/src/img/user_menu_avatar.png"
                                alt="Contact"
                            />
                        </div>
                        <div class="col pl-0">
                            <span
                                t-esc="contact"
                                t-options='{"widget": "contact", "fields": ["name", "phone"], "no_marker": True}'
                            />
                        </div>
                    </div>
                </t>
            </li>
        </xpath>
        <xpath expr="//li[@id='sidebar_partner']" position="attributes">
            <attribute name="t-if">1==0</attribute>
        </xpath>
        <xpath
            expr="//ul[@id='survey_portal_sidebar_list']//div[hasclass('o_download_pdf')]"
            position="inside"
        >
            <div class="btn-group flex-grow-1 mr-1 mb-1">
                <button
                    type="button"
                    id="modal_invite_contacts"
                    class="btn btn-secondary btn-block"
                    t-att-answer-id="answer.id"
                    t-att-data-token="answer.access_token"
                    t-att-data-survey="answer.survey_id.access_token"
                >
                    <i class="fa fa-address-book" /> Add Contacts
                </button>
            </div>
            <div class="btn-group flex-grow-1 mr-1 mb-1">
                <button
                    type="button"
                    id="modal_survey_contact_invites"
                    class="btn btn-secondary btn-block"
                    t-att-answer-id="answer.id"
                    t-att-data-token="answer.access_token"
                    t-att-data-survey="answer.survey_id.access_token"
                >
                    <i class="fa fa-address-book" /> Show invites
                </button>
            </div>
        </xpath>
    </template>

    <template id="show_invites_modal" name="Invite Contacts Modal">
        <div
            id="modal_survey_contact_invites"
            class="modal fade"
            tabindex="-1"
            role="dialog"
        >
            <div class="modal-dialog modal-lg" role="document">
                <div class="modal-content">
                    <div class="modal-header align-items-center">
                        <h4 class="modal-title">Survey answer invites</h4>
                        <button
                            type="button"
                            class="close close-modal"
                            data-bs-dismiss="modal"
                            aria-label="Close"
                        >
                            <span>x</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        <div class="row">
                            <div class="col-12">
                                <table class="table table-hover">
                                    <thead>
                                        <tr>
                                            <th scope="col">Created at</th>
                                            <th scope="col">Invited by</th>
                                            <th scope="col">Email</th>
                                            <th scope="col">Link</th>
                                            <th scope="col">Used by</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-foreach="answer.invite_ids" t-as="invite">
                                            <tr>
                                                <td>
                                                    <span
                                                        t-field="invite.create_date"
                                                    />
                                                </td>
                                                <td><t
                                                        t-esc="invite.create_uid.name"
                                                    /></td>
                                                <td><t t-esc="invite.email" /></td>
                                                <td>
                                                    <a
                                                        t-attf-href="/survey/invite/code/{{invite.code}}"
                                                    >Link</a>
                                                </td>
                                                <td><t
                                                        t-esc="invite.user_id.name or '-'"
                                                    /></td>
                                            </tr>
                                        </t>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button
                            type="button"
                            class="btn btn-secondary"
                            data-bs-dismiss="modal"
                        >Cancel</button>
                    </div>
                </div>
            </div>
        </div>
    </template>
</odoo>
