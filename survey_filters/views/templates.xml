<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="survey_filter_assets" name="Survey filter assets">
        <script
            type="text/javascript"
            src="/survey_filters/static/src/js/survey_filter.js"
        />
    </template>

    <template id="survey_page_statistics_inner" inherit_id="survey.survey_page_statistics_inner">
    <xpath expr="//div[@class='card d-print-none']" position="before">
        <t t-call-assets="survey_filters.survey_filter_assets" lazy_load="True" />
        <div class="container pb-2 pt-2">
            <div class="row">
                <!-- Suodatinvalikot -->
                <div class="col-sm-4 mt-1 mb-1">
                    <div class="input-group date" id="datetimepicker-filter-date" data-target-input="nearest">
                        <input
                            type="text"
                            data-date-format="yyyy/mm/dd"
                            name="filter_date"
                            id="filter-date"
                            class="form-control datetimepicker-input"
                            data-target="#datetimepicker-filter-date"
                            t-att-value="select_date or ''"
                        />
                        <div
                            class="input-group-append"
                            data-date-format="yyyy/mm/dd"
                            data-target="#datetimepicker-filter-date"
                            data-toggle="datetimepicker"
                        >
                            <div class="input-group-text"><i class="fa fa-calendar" /></div>
                        </div>
                    </div>
                </div>

                <div class="col-sm-4 mt-1 mb-1">
                    <div class="input-group date" id="datetimepicker-filter-end-date" data-target-input="nearest">
                        <input
                            type="text"
                            data-date-format="yyyy/mm/dd"
                            name="filter_end_date"
                            id="filter-end-date"
                            class="form-control datetimepicker-input"
                            data-target="#datetimepicker-filter-end-date"
                            t-att-value="date_end or ''"
                        />
                        <div
                            class="input-group-append"
                            data-date-format="yyyy/mm/dd"
                            data-target="#datetimepicker-filter-end-date"
                            data-toggle="datetimepicker"
                        >
                            <div class="input-group-text"><i class="fa fa-calendar" /></div>
                        </div>
                    </div>
                </div>

                <div class="col-sm-4 mt-1 mb-1">
                    <select
                        class="form-control input-lg"
                        name="select_course"
                        id="select_course"
                        multiple="multiple"
                    >
                        <t t-foreach="courses" t-as="c">
                            <option
                                t-att-value="c.id"
                                t-att-selected="'selected' if selected_courses and str(c.id) in selected_courses.split(',') else ''"
                            >
                                <t t-esc="c.name" />
                            </option>
                        </t>
                    </select>
                </div>

                <div class="col-sm-4 mt-1 mb-1">
                    <select
                        class="form-control input-lg"
                        name="selectevent"
                        id="selectevent"
                        multiple="multiple"
                    >
                        <t t-foreach="events" t-as="e">
                            <option
                                t-att-value="e.id"
                                t-att-selected="'selected' if selected_events and str(e.id) in selected_events.split(',') else ''"
                            >
                                <t t-esc="e.name" />
                            </option>
                        </t>
                    </select>
                </div>

                <!-- Päivitä-painike -->
                <div class="col-sm-4 mt-1 mb-1">
                    <button id="apply_filters" class="btn btn-primary w-100">Päivitä/aktivoi</button>
                </div>
            </div>
        </div>
    </xpath>
</template>

</odoo>
